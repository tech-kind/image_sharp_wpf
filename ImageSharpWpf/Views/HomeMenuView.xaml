<UserControl
    x:Class="ImageSharpWpf.Views.HomeMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:wpfui="clr-namespace:WPFUI.Controls;assembly=WPFUI"
    xmlns:wpfuiCommon="clr-namespace:WPFUI.Common;assembly=WPFUI"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <Menu>
        <MenuItem
            Command="{Binding FileSelectCommand}"
            Header="画像選択"
            Icon="{x:Static wpfuiCommon:Icon.Open20}" />
        <MenuItem Header="変換">
            <MenuItem Command="{Binding RGBToBGRCommand}" Header="チャンネル変換" />
            <MenuItem Command="{Binding GrayScaleCommand}" Header="グレースケール" />
            <MenuItem Command="{Binding HSVCommand}" Header="HSV変換" />
        </MenuItem>
        <MenuItem Header="減色">
            <MenuItem Command="{Binding ThresholdCommand}" Header="二値化" />
            <MenuItem Command="{Binding OtsuThresholdCommand}" Header="大津の二値化" />
            <MenuItem Command="{Binding SubtractionCommand}" Header="減色処理" />
        </MenuItem>
        <MenuItem Header="フィルタ">
            <MenuItem Command="{Binding AveragePoolingCommand}" Header="平均値プーリング" />
        </MenuItem>
    </Menu>
</UserControl>
